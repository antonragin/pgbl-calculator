services:
  - type: web
    name: pgbl-calculator
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx next start -p ${PORT:-10000}
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: SITE_PASSWORD
        value: "OryxRulezzz2026!"
      - key: PORT
        value: "10000"
